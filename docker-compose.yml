services:
  beekeeper_api:
    container_name: beekeeper_api 
    image: beekeeper_api
    restart: always
    build:
      context: ./api
      dockerfile: Dockerfile
    ports:
      - "8002:8080"
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      .env
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/api/health"]
      interval: 5s
      retries: 5
      start_period: 10m
    mem_limit: 1g
    cpus: "2.0"

  postgres:
    image: postgres:18
    container_name: beekeeper_database
    restart: always
    entrypoint: ["/bin/sh", "/usr/local/bin/start-postgres.sh"]
    environment:
      POSTGRES_USER: ${DB_USER:-beekeeper}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB:-beekeeper}
    volumes:
      - postgres_data:/var/lib/postgresql
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./db/start-postgres.sh:/usr/local/bin/start-postgres.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U beekeeper -d beekeeper"]
      interval: 5s
      retries: 5
    mem_limit: 1g
    cpus: "2.0"

  beekeeper_cron:
    container_name: beekeeper_cron
    image: alpine-base
    mem_limit: 1g
    cpus: "2.0"
    restart: always
    build:
      context: ./cron
      dockerfile: Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DB_USER: ${DB_USER:-beekeeper}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB:-beekeeper}
      DB_HOST: postgres

volumes:
  postgres_data:
